Download and install [Laragon](https://laragon.org/download) to manage your MySQL database and local server environment.

Start MySQL Service.

Login as root user
```bash
$ mysql -u root
```

Use CLI, 
```bash
mysql> CREATE DATABASE campus_db
```

created the database campus_db in Laragon. Then other tables will be created by django migration.